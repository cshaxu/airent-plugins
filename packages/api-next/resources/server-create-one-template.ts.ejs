<% if (entity.api && entity._booleans.api.hasCreateOne) { -%>
// entity imports
import <%= `${utils.toPascalCase(entity.name)}Handler` %> from '<%- entity._packages.apiNext.routeToHandlerFull %>';

export const POST = <%= `${utils.toPascalCase(entity.name)}Handler` %>.createOne;
<% } -%>
