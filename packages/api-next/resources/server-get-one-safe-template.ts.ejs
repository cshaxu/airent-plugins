<% if (entity.api && entity._booleans.api.hasGetOneSafe) { -%>
// entity imports
import <%= `${utils.toPascalCase(entity.name)}Handler` %> from '<%- entity._packages.apiNext.routeToHandlerFull %>';

export const POST = <%= `${utils.toPascalCase(entity.name)}Handler` %>.getOneSafe;
<% } -%>
