<% if (entity.api && entity._booleans.api.hasDeleteOne) { -%>
// entity imports
import <%= `${utils.toPascalCase(entity.name)}Handler` %> from '<%- entity._packages.apiNext.routeToHandlerFull %>';

export const POST = <%= `${utils.toPascalCase(entity.name)}Handler` %>.deleteOne;
<% } -%>
